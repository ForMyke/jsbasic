//Acceso a un formulario
const correo = document.forms["formulario-donacion"]["correo"].value;
//Acceso al pais
const pais = document.forms["formulario-donacion"]["pais"].value;
//Acceso al valor
const valor = document.forms["formulario-donacion"]["donacion"].value;
//Acceso a la fecha
const fecha = document.forms["formulario-donacion"]["fecha"].value;
//Acceso al formulario
const formulario = document.forms["formulario-donacion"];
btnEnviar = document
  .getElementById("btnEnviar")
  .addEventListener("click", (evento) => {
    evento.preventDefault();

    //Acceso a los Elementos
  });
